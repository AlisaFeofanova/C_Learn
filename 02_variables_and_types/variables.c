#include <stdio.h>

int main() {
    // Declaring variables of different types
    int age = 25;
    float height = 5.9;
    char initial = 'J';

    // Printing values
    printf("Age: %d\n", age);
    printf("Height: %.1f\n", height);
    printf("Initial: %c\n", initial);

    return 0;
}